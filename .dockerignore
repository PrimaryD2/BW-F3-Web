# Git history
.git
.gitignore

# Node modules — rebuilt inside Docker, never send to build context
**/node_modules

# Local secrets — never bake into the image
**/.env
.env
*.env.local

# Logs
*.log
npm-debug.log*

# OS / editor noise
.DS_Store
Thumbs.db
.vscode
.idea
