services:

  # ── MariaDB ──────────────────────────────────────────────────────────────────
  db:
    image: mariadb:11.4
    container_name: f3-db
    restart: unless-stopped
    environment:
      MARIADB_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-rootpassword}
      MARIADB_DATABASE:      ${DB_NAME:-f3_production}
      MARIADB_USER:          ${DB_USER:-f3user}
      MARIADB_PASSWORD:      ${DB_PASS:-f3password}
    volumes:
      - f3-db-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 30s

  # ── phpMyAdmin ───────────────────────────────────────────────────────────────
  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: f3-phpmyadmin
    restart: unless-stopped
    ports:
      - "8081:80"
    environment:
      PMA_HOST: db
      PMA_PORT: 3306
      UPLOAD_LIMIT: 512M
    depends_on:
      db:
        condition: service_healthy

  # ── Node.js API + served React app ───────────────────────────────────────────
  server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: f3-server
    restart: unless-stopped
    ports:
      - "${PORT:-3001}:3001"
    volumes:
      - ./server/uploads:/app/server/uploads
    depends_on:
      db:
        condition: service_healthy
    environment:
      NODE_ENV: production
      PORT:     3001
      DB_HOST:  db
      DB_PORT:  3306
      DB_NAME:  ${DB_NAME:-f3_production}
      DB_USER:  ${DB_USER:-f3user}
      DB_PASS:  ${DB_PASS:-f3password}
      JWT_SECRET: ${JWT_SECRET:-change-this-to-a-long-random-secret-before-deploying}
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3001/api/health"]
      interval: 15s
      timeout:  10s
      retries:  5
      start_period: 60s

volumes:
  f3-db-data: